[package]
name = "todo_list"
version = "0.0.1"
authors = ["24CSE179"]
license = "MIT"
edition = "2024"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework", rev = "e4f3c5d" }

[addresses]
todo_list = "0x0"

[dev-dependencies]
# Add test-only dependencies here if needed

[dev-addresses]
# Override named addresses for testing if needed
